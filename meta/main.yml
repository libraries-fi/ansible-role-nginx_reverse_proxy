---
allow_duplicates: yes
dependencies:
# TODO: can't easily use the group_by stuff here AFAIK so directly including squeeze...
  - role: ajsalminen.nginx
    nginx_proxy: true
  - role: ajsalminen.nginx_common
